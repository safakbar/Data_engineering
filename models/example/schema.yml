
version: 2

sources:
  - name: stage
    database: DBTPROJE          # Snowflake database adın
    schema: stage               # stage schema'n
    tables:
      - name: stocks


models:
    - name: my_first_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: my_second_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
